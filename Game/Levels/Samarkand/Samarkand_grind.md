1. L01. directly closed by `grind`, but not `grind (ematch := 0)`.
2. L02. directly closed by `grind`, but not `grind (ematch := 0)`.
3. L04. after `obtain ⟨a, ha⟩ := hf b` then `grind`.
4. L05. Line 35 after `use a` then `grind`.
5. L08. directly by `grind`, not `grind (ematch := 0)`.
6. L09. we already have your solution.
