1. L08: `rw [if_pos h₂]` can be replaced by `grind`,
  `rw [if_neg h₂]; rw [zero_on_offDiag_ebasis]` can be replaced by `grind [zero_on_offDiag_ebasis]`.
2. L09: line 176 `grind [map_sum, eq_on_diag_ebasis] `, line 190 `grind [ebasis_diag_sum_eq_one]`
