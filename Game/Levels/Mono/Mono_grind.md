1. L01. `grind` after `intro`.
2. L02. already know how to change to statement setup.
3. L04. `grind` after `intro`.
4. L06. very small golfing `grind [hf hlt]`.
5. L09. after `obtain ⟨g, hg⟩ := h -- line 24` then `grind`.
6. L10. the first case in line 39, can be solved by `grind`.
7. L11. Line 69 after `use g` `grind` solves the rest. Line 88
after `obtain ⟨g, hg⟩ := hL` then `grind`.
