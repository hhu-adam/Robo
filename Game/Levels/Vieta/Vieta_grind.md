1. L04: after `use fun (x : ℤ) ↦ x - 3` then `grind`. This is used to introduce `comp_apply`.
2. L05: already change the problem to avoid `grind` solves the goal.
3. L06: only need `grind`. Even before `funext` and `grind (ematch := 0)`.
4. L10. after `specialize hs b`, then `grind`.
